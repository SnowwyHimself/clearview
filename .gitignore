.venv/
__pycache__/
*.pyc
jobs/
scratch_*
# Build outputs and fetched binaries (regenerated by the build scripts / CI)
build/
dist/
build_assets/
*.dmg
*.iconset/
# Machine-specific static ffprobe fetched for local dev (see README)
bin/
# Local editor/harness config (contains machine-specific paths)
.claude/
